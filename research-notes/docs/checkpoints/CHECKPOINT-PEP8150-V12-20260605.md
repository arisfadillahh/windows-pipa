# CHECKPOINT - PEP8150 v12 test staged

## Current device state

- PC does not see adb or fastboot devices.
- PC sees the tablet Windows partition mounted as `E:` with label `WINPIPA`.
- Do not flash until `fastboot devices -l` shows the Xiaomi Pad 6.

## Result from previous test

- `pad5-spi-only` did not complete.
- It crashed/rebooted at `pnputil.exe /add-driver C:\woa\pad5-spi-only\driver\MiPad5_spi.inf /install`.
- No `RESULT.txt` or `after` dump was produced.
- Startup fallback was already removed and the test folder was marked disabled offline.
- Conclusion: Pad5 QCSPI also crashes at start, so the blocker is lower than SPI driver choice.

## New firmware built

- ACPI edit: `TouchMinSSDT.asl` changed `PEP0 _HID` from `QCOM2519` to native pipa `QCOM0519`.
- Build log: `external\Mu-Qcom\build-pipa-touchmin-v12-pep0519-inner.log`
- Build result: success.
- Artifacts:
  - `<ARTIFACT_DIR>\pipa_muold_touchmin_v12-pep0519.fd`
  - `<ARTIFACT_DIR>\pipa_muold_touchmin_v12-pep0519.img`
- Hashes:
  - FD SHA256 `CB5C0BA13C4315C7C11855AB6ED6DBADE9FAF0D93D710A6C87CF24444BF703D5`
  - IMG SHA256 `EDA58156B1E34C87DC2D0AECA13CFED15FB6A72D2878BF287210B637D3994778`

## New Windows-side test staged

- Script:
  - `E:\woa\oneshot\pep8150\Test-PipaPep8150Only.ps1`
  - `E:\woa\oneshot\pep8150\Run-PipaPep8150Only.cmd`
  - `E:\woa\pep.cmd`
  - `E:\woa\pep8150.cmd`
- Driver staged:
  - `E:\woa\oneshot\drivers\qcpep8150\qcpep.wp8150.inf`
  - `E:\woa\oneshot\drivers\qcpep8150\qcpep8150.cat`
  - `E:\woa\oneshot\drivers\qcpep8150\qcpep8150.sys`

## Next step when true fastboot is detected

1. Flash v12 image to Windows slot boot partition:
   `fastboot flash boot_b "<ARTIFACT_DIR>\pipa_muold_touchmin_v12-pep0519.img"`
2. Boot Windows slot:
   `fastboot set_active b`
   `fastboot reboot`
3. In Windows, run:
   `C:\woa\pep.cmd`
4. Approve UAC if shown.
5. After the script finishes or after any reboot/crash, return to fastboot/mass-storage and collect:
   `C:\woa\oneshot\pep8150\RESULT.txt`

## Pass condition

- `ACPI\QCOM0519` binds to `qcpep` and becomes `CM_PROB_NONE`.
- `sc query qcpep` is not missing and does not show a failed add/start status.

## Fail condition

- Reboot/BSOD during PEP install.
- `ACPI\QCOM0519` stays Code 28/31 or problem status `0xc000000e`.

