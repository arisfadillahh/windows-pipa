<?xml version="1.0" encoding="utf-8"?>
<!-- EWDK/WDK KMDF driver project for pipakbd (ARM64). Build from an EWDK build env:
     msbuild pipakbd.vcxproj /p:Configuration=Release /p:Platform=ARM64 -->
<Project DefaultTargets="Build" ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Release|ARM64">
      <Configuration>Release</Configuration>
      <Platform>ARM64</Platform>
    </ProjectConfiguration>
  </ItemGroup>
  <PropertyGroup Label="Globals">
    <ProjectGuid>{8F3A1C42-9E21-4B7C-A0D5-1F2E3C4B5A60}</ProjectGuid>
    <TargetVersion>Windows10</TargetVersion>
    <DriverType>KMDF</DriverType>
    <RootNamespace>pipakbd</RootNamespace>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <PropertyGroup Label="Configuration">
    <ConfigurationType>Driver</ConfigurationType>
    <PlatformToolset>WindowsKernelModeDriver10.0</PlatformToolset>
    <UseDebugLibraries>false</UseDebugLibraries>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <ItemDefinitionGroup>
    <ClCompile>
      <WppEnabled>false</WppEnabled>
      <!-- _KERNEL_MODE is defined automatically by the kernel toolset; do not redefine it
           (C4117 reserved-macro warning becomes fatal under /WX). -->
      <!-- spbcx.h is in the versioned km\spb\1.0 subfolder which the toolset doesn't add.
           Path is stable because the WDK NuGet version is pinned in packages.config. -->
      <AdditionalIncludeDirectories>$(MSBuildThisFileDirectory)packages\Microsoft.Windows.WDK.arm64.10.0.28000.1839\c\Include\10.0.28000.0\km\spb\1.0;%(AdditionalIncludeDirectories)</AdditionalIncludeDirectories>
    </ClCompile>
    <Link>
      <!-- vhfkm.lib (Virtual HID Framework, kernel) + ntstrsafe.lib (RtlStringCchPrintfW).
           No spbcx.lib: this is an SPB *client* (talks to the I2C controller via WdfIoTarget
           read/write), not an SPB controller, so it links no SpbCx exports. KMDF entry libs
           (WdfDriverEntry/WdfLdr) are added automatically by the KMDF toolset. -->
      <AdditionalDependencies>ntstrsafe.lib;vhfkm.lib;%(AdditionalDependencies)</AdditionalDependencies>
      <!-- $(ExtraLibDir) is supplied by the CI build (discovered NuGet WDK lib dirs for
           spbcx.lib/vhfkm.lib that aren't on the default lib path). -->
      <AdditionalLibraryDirectories>$(ExtraLibDir);%(AdditionalLibraryDirectories)</AdditionalLibraryDirectories>
    </Link>
    <Inf>
      <SpecifyArchitecture>true</SpecifyArchitecture>
      <Architecture>arm64</Architecture>
    </Inf>
  </ItemDefinitionGroup>
  <ItemGroup>
    <ClCompile Include="driver.c" />
    <ClCompile Include="device.c" />
    <ClCompile Include="frame.c" />
    <ClCompile Include="spb.c" />
    <ClCompile Include="vhf.c" />
  </ItemGroup>
  <ItemGroup>
    <ClInclude Include="pipakbd.h" />
    <ClInclude Include="report_descriptor.h" />
  </ItemGroup>
  <ItemGroup>
    <Inf Include="pipakbd.inf" />
  </ItemGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
